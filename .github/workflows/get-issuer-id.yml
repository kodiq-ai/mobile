name: Get Issuer ID
on:
  workflow_dispatch:
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - name: Output Issuer ID
        env:
          ISSUER: ${{ secrets.APPSTORE_CONNECT_ISSUER_ID }}
        run: echo "ISSUER_ID=$ISSUER"
